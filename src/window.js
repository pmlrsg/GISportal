/**
 * Window
 * @namespace
 */
opec.window = {};

opec.window._openWindows = [];
opec.window._windowTypes = [];

opec.window.addWindow = function(window) {
   
}

opec.window.showWindow = function(windowType, uid, data) {
   
}

opec.window.hideWindow = function(uid) {
   
}

opec.window.addWindow({
   name: 'accordionCreation',
   create: function(uid, data) {
      
   },
   destroy: function() {
      
   }
})
