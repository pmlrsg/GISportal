{{#unless browser}}
	{{#unless insecure}}
		<div class="collaboration-actions">
		   <button class="js-toggle-rtc brand secondary extra-small gray"><span class="icon-webcam"></span><span class="btn-value">Enable Audio/Video</span></button>
		</div>

		<p>People With Audio/Video Enabled:</p>

		<div class="video-people-list"></div>
	{{else}}
		<p>Audio/Video is not available on insecure origins.</p>
	{{/unless}}
{{else}}
	<p>Audio/Video is not available on this browser.</p>
{{/unless}}